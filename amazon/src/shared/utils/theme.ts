import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#FF9900",
      contrastText: "#0000",
    },
    secondary: {
      main: "#0000",
      contrastText: "#FFFF",
    },
  },
});
